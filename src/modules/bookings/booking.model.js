/**
 * bookings collection — schema reference
 * { _id, classId, className, trainerName, trainerEmail,
 *   userEmail, userName, schedule, price, transactionId, createdAt }
 */
module.exports = { collectionName: 'bookings' };