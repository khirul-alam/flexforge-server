/**
 * trainerApplications collection — schema reference
 * { _id, userEmail, userName, experience, specialty,
 *   status: 'pending' | 'approved' | 'rejected', feedback, createdAt }
 */
module.exports = { collectionName: 'trainerApplications' };